import { Poppins } from "next/font/google";
import { Fira_Code } from "next/font/google";

export const poppins = Poppins({weight: ["400", "700"], subsets: "latin"})
export const fira_code = Fira_Code({weight: ["400", "700"], subsets: "latin"})
